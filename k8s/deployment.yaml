apiVersion: apps/v1
kind: Deployment
metadata:
  name: recipebox
  namespace: colossus-recipebox
  labels:
    app: recipebox
    managed-by: colossus
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: recipebox
  template:
    metadata:
      labels:
        app: recipebox
    spec:
      hostAliases:
        - ip: "192.168.50.250"
          hostnames:
            - "ubuntu.desmana-truck.ts.net"
      containers:
        - name: app
          image: ubuntu:30500/recipebox:latest
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: app-secrets
                optional: true
